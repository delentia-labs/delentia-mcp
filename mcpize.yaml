# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "delentia-mcp"
slug: "delentia-mcp"
description: "Stop rogue tool calls & AI hallucinations with deterministic mathematical guardrails (ZK-FDIA), RCT-7 reasoning, Delta context compression, and swarm orchestration."

runtime: nodejs

build:
  install: "npm install"
  command: "npm run build"

start:
  type: stdio
  command: "node bin/cli.js"

bridge:
  mode: stdio
